apiVersion: v1
kind: Service
metadata:
  name: {{ template "viking.fullname" . }}
  labels:
    app: {{ template "viking.name" . }}
    chart: {{ template "viking.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "viking.name" . }}
    release: {{ .Release.Name }}
